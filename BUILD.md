## Backend build

Use `pip install -r requirements.txt` to update dependencies.

Use `pip install .` in the root folder to install modules.

use `node install` from the frontend root folder followed by  `npm run dev` to run the frontend.
